<h3><%= image_tag post.author.avatar.variant(resize: "40x40!") %> <%= post.author.name %></h3>
<p><%= post.body %></p>
<p><%= pluralize(post.like_count, 'like') %> <%= link_to "like", like_post_path(post), method: 'post' %> <%= link_to "unlike", unlike_post_path(post), method: 'delete' %></p>