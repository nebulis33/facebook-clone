<ul>
    <% @users.each do |user| %>
        <li><%= user.name %> <%= render partial: 'shared/friend_request_button', locals: {user: user} %></li>
    <% end %>
</ul>

<%# really need to fix the querying here, bigtime n+1 %>