<ul>
    <% @users.each do |user| %>
        <li><%= image_tag user.avatar.variant(resize: "60x60!") %><%= link_to user.name, user_path(user) %> <%= render partial: 'friend_status', locals: {user: user} %></li>
    <% end %>
</ul>